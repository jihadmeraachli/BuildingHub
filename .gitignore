# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local state
supabase/.temp

# Claude Code local settings
.claude/settings.local.json
.claude/

# Capacitor native projects (generated per-machine via npx cap add)
ios/
android/

# Local database snapshots (scripts/dump-db.mjs) — every resident's real data.
# Never commit, never put on a shared drive.
db-dumps/
